/** Server actions use this string; UI maps it to `elevator.duplicateName` (see ProjectElevatorSettings). */
export const elevatorDuplicateMessage =
  "Un elevateur avec ce nom existe deja sur ce chantier.";
